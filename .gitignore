# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
keys/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
data/audit/

# Cursor local hook runtime (continual-learning index/state)
.cursor/hooks/state/

# Frontend (isolated — npm only inside frontend/)
frontend/node_modules/
frontend/.next/
frontend/out/

# Video subproject (~574 MB) — not part of the deployed app; ignore entirely
sentinel-video/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*.orig
